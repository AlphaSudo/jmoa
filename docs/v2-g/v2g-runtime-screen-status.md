# V2-G Runtime Screen Status

Target:

```text
Doctor corrected D2
```

Status:

```text
NOT_RUN
```

Reason:

```text
Semantic smoke was blocked.
The required Doctor container images are not currently present.
The corrected D2 CDS archive also needs explicit handling before it can be used
with the reduced artifact.
```

Therefore:

```text
V2-C validation: not run
V2-D attribution: not run
cross-service runtime claim: false
```

