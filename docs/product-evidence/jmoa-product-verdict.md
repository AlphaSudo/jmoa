# JMOA Direct Product Verdict

Aggregate state: `DIRECT_PRODUCT_MATRIX_INCOMPLETE_ENVIRONMENT_BLOCKED`.

JMOA V2 has one substantial clean no-JMOA-to-final-V2 confirmation on Doctor:
median PSS `-5,809 KB`, Private_Dirty `-5,492 KB`, and `memory.current`
`-11,845,632` bytes across three winning pairs.

PetClinic's latest signed campaign ran B0 first and stopped before any V2 arm.
The same clean B0 artifact exceeded the frozen same-artifact noise gate in two
complete campaign executions. Patient's direct screens used a candidate SHA
that differs from the accepted corrected V2 artifact. Neither service receives
direct product-win or product-regression language.

The Doctor measurement remains confirmed. The aggregate adoption verdict is
incomplete because PetClinic requires a dedicated Linux campaign and Patient
requires an accepted-artifact comparison. The PetClinic stop is evidence
discipline, not a hidden failure: eight B0 arms, 648 requests, and every command
response are retained in private integrity-indexed ledgers.

The three-service V1-to-V2 matrix remains a successful engineering-evolution
result. It is reported separately and is not used to inflate the direct product
verdict.
