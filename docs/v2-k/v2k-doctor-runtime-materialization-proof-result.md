# V2-K Doctor Runtime Materialization Proof Result

Status:

```text
NOT_ATTEMPTED_BLOCKED
```

Runtime materialization proof requires runnable baseline and candidate images.
Those images are not currently available, so no runtime image was inspected.

The local fat-JAR materialization remains artifact-level proof only:

```text
BOOT-INF/lib entries replaced: 184/184
runtime image proof: not attempted
```

No runtime materialization proof means no valid Doctor semantic smoke.
