# v0.8.4 - V2-K Doctor Runtime Blocked With Root Cause

This release closes the Doctor runtime recovery sub-phase as:

```text
BLOCKED_WITH_ROOT_CAUSE
```

The recovery audit found that legacy Doctor runtime assets exist, but they are
not directly reusable as public/productized V2-K runtime infrastructure.

What was found:

```text
legacy runtime assets found
corrected D2 artifact present
D2 + raw reducer artifact present
corrected D2 CDS archive present
```

What remains blocked:

```text
D2R CDS archive not trained
private runtime inputs not configured
required runtime images missing
runtime network missing
runtime materialization proof not available
semantic smoke not attempted
runtime screen not attempted
```

This is not a reducer failure. It is a runtime infrastructure and policy
blocker.

Next allowed V2-K move:

```text
start Spring PetClinic visits-service public fallback lane
```

No Doctor runtime, startup, CDS, V2-C, V2-D, or memory claim is made.
