# v0.16.2 - V2-U Generated Evidence Partial

V2-U adds the matched generated-family evidence campaign layer.

This release includes:

- `scripts/capture-generated-lifecycle.ps1` for startup, warmup, and workload
  generated-family diagnostic capture.
- Full static/capture identity tuple enforcement for generated-family evidence:
  artifact SHA-256, service, launch mode, runtime policy, reducer engine,
  family registry version, and scanner version.
- Manifest-aware `jmoa:analyze-generated-evidence` support.
- V2-U JSON/Markdown reports plus compatibility V2-T report filenames.
- Recommendation ingestion that treats generated-family runtime relevance as
  valid only when evidence status is `MATCHED_DIAGNOSTIC_EVIDENCE`.
- V2-U closure docs for customers, visits, Doctor D2R, lifecycle reconciliation,
  cross-service matrix, matched ROI ranking, and prototype admission.

Closure:

```text
PARTIAL_INFRASTRUCTURE
```

Current local evidence remains incomplete:

- Customers has historical diagnostic signal but lacks the full matched
  fingerprinted startup/warmup/workload lifecycle bundle.
- Visits has reducer/runtime evidence but lacks a matching generated-family
  inventory and lifecycle bundle.
- Doctor D2R has static generated-family inventory but lacks a fresh matched
  D2R lifecycle bundle.

No generated-family prototype is admitted. No generated-class mutation is
enabled. No runtime performance claim is added.
