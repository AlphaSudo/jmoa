# v0.10.0 - V2-L Public Runtime Confirmed

V2-L confirms the productized raw LVT/LVTT reducer on Spring PetClinic
visits-service, a second public runtime target.

Comparison:

```text
public visits-service baseline
vs
the same baseline + raw LVT/LVTT reducer
```

Protocol:

```text
EXPLODED_BOOT_APP / JarLauncher
NO_CDS_LOW_DIRTY
MALLOC_ARENA_MAX=1
no CDS/AppCDS/Leyden
no runtime javaagent
Java 17 container runtime
embedded HSQLDB
```

Confirmation:

```text
V2-C verdict: CONFIRMED_WIN
valid runs: 6/6
paired wins: 3/3
median PSS delta: -2,012 KB
median Private_Dirty delta: -1,680 KB
median memory.current delta: -1,712,128 bytes
```

Artifact result:

```text
dependency-layer compressed-byte delta: -3,515,600 bytes
classes reduced and byte-audited: 29,701
failed audits: 0
```

V2-D attributes the result primarily to lower `anonymous_rw` PSS and
NMT-visible metaspace committed memory. Heap PSS increased, so this is not a
heap-page or retained-object reduction claim.

Boundary:

```text
This is baseline vs baseline + reducer, not a visits full-P2 comparison.
No startup, fat-JAR, CDS/AppCDS, all-PetClinic, or universal Spring Boot claim.
```
