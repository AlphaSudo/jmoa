# v0.8.5 - V2-K Doctor Runtime Recovered

This release supersedes the earlier Doctor runtime blocked state.

V2-K recovered the local Doctor runtime stack by locating the private runtime
inputs, rebuilding support images from already-built jars, training a fresh D2R
CDS archive for the raw-reduced artifact, and validating semantic smoke.

Recovered:

```text
private config input available locally
Doctor DB init input available locally
Java 26 runtime image available locally
Postgres runtime image available locally
config/discovery/D2/D2R images rebuilt locally
fresh D2R CDS archive trained
D2R health UP with fresh CDS
secured Doctor endpoint HTTP 200
```

Fresh D2R CDS archive:

```text
bytes: 126,636,032
sha256: 64A4331695D092148A105ADAA47FEEA0CA46CB0CC561C3289F0413D1A67B6ACC
```

Claim boundary:

```text
semantic/runtime recovery only
no Doctor memory claim
no startup claim
no V2-C confirmation
no V2-D attribution
```

Next gate:

```text
Doctor corrected D2 vs Doctor corrected D2R single-screen measurement.
```
