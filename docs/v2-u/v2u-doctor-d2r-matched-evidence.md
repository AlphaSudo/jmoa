# V2-U Doctor D2R Matched Evidence

Status: `PARTIAL_INFRASTRUCTURE`

Doctor D2R is valuable because it covers a private Spring Boot fat-JAR/CDS
protocol with a different service surface. The current V2-A/V2-T generated data
is static-only.

Current usable signals:

```text
unique generated-like classes: 14,469
unique generated-like classfile bytes: 96,525,651
```

Missing for V2-U admission:

```text
startup attribution capture
warmup attribution capture
workload attribution capture
capture artifact SHA-256
CDS archive SHA-256 in the generated lifecycle manifest
CDS mapping proof attached to the diagnostic bundle
full V2-U identity tuple
```

Verdict:

```text
LIFECYCLE_CAPTURE_INCOMPLETE
prototypeAdmitted=false
```

Doctor generated-family diagnostics must be captured in dedicated diagnostic
sessions. Existing V2-K claimable memory pairs must remain class-load logging
free.
