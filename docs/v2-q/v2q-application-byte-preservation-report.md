# V2-Q Application Byte Preservation

The raw application reducer removed only `LocalVariableTable` and
`LocalVariableTypeTable` metadata from four admitted ordinary classes.

```text
application bytes removed: 480
raw audits passed: 4
raw audits failed: 0
LineNumberTable, SourceFile, StackMapTable, annotations, signatures,
and BootstrapMethods: preserved for every audited class
```
