# V2-V Lifecycle Matrix

The matrix is generated only from matched bundles. It reports packaged,
startup, warmup-new, workload-new, runtime-generated, and histogram-persistent
counts for each generated family.

An incomplete target can appear in the bundle report, but it contributes no
runtime-relevance admission signal to the matched matrix.
