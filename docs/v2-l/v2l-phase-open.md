# V2-L Phase Boundary

V2-L was opened to test the productized raw reducer on a second public runtime
target after the private Doctor V2-K confirmation.

Target:

```text
Spring PetClinic visits-service
public source revision: 305a1f13e4f961001d4e6cb50a9db51dc3fc5967
```

Comparison selected after inventory:

```text
visits baseline
vs
the same visits baseline + raw LVT/LVTT reducer
```

A visits-specific full-P2 artifact was not available. V2-L therefore does not
borrow the customers-service P2 profile and does not describe this result as an
incremental win over full P2.

Runtime claim gate:

```text
public source and build
artifact reduction with zero failed byte-preservation audits
runtime materialization proof
semantic smoke with zero workload/linkage errors
single-screen promotion
three valid pairs
V2-C CONFIRMED_WIN
V2-D attribution
```

Final phase status:

```text
CLOSED_CONFIRMED
```
