# Phase V2-A8 Documentation And Public Story

Status: documentation updated.

V2-A public wording:

```text
JMOA V2-A inventories generated/JVM synthetic/proxy/AOT class shapes, attributes
runtime presence from class-load logs and class histograms, classifies families
with a safety taxonomy, and selects Spring AOT BeanDefinition helpers as the
first report-only prototype family.
```

Do not claim:

```text
JMOA optimizes all synthetic classes.
JMOA rewrites CGLIB/JDK proxy classes.
JMOA has confirmed generated-class memory wins.
```

Current claim:

```text
V2-A adds the generated-class visibility, attribution, and safety infrastructure
needed before any generated-class bytecode optimizer can be safely enabled.
```
