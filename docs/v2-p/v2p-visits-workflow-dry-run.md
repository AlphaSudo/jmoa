# V2-P Visits Workflow Dry Run

V2-P uses the already-confirmed V2-L visits-service protocol as a compatibility
dry run. It verifies that the public no-CDS raw-reducer workflow maps cleanly
to the golden workflow without rerunning containers or making a new claim.
